{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/Engine",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "preLaunchTask": "build"
    }
  ]
}